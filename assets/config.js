/* ====== НАСТРОЙКИ ======
   adminIds: числовые Telegram ID админов — только они видят админ-панель.
   apiUrl:   адрес Cloudflare Worker с проверкой теории (см. bot/DEPLOY-BOT.md),
             например "https://linal-quiz-bot.ТВОЙ-ПОДДОМЕН.workers.dev".
             Пусто — проверка ИИ выключена, работает самопроверка по эталону.
   exam:     параметры экзамена по билетам и шкала оценок.                  */
window.QUIZ_CONFIG = {
  adminIds: [5185408627],   // твой Telegram ID — админка включается автоматически
  version: "2.0",
  apiUrl: "https://linal-quiz-bot.linal-quiz-bot.workers.dev",
  exam: {
    tickets: 2,             // сколько билетов выпадает
    theoryMinutes: 45,      // ориентир по времени на письменную часть (не ограничивает)
    testsPerTicket: 6,      // тестовых вопросов по каждому билету
    testMinutes: 12,        // время на тестовую часть
    theoryWeight: 0.6,      // вес теории в итоговом балле (тесты = 1 − вес)
    minTicketTheory: 30,    // если теория хотя бы одного билета ниже — оценка 2
    grade5: 85,             // итоговый балл (%) для «отлично»
    grade4: 70,             // для «хорошо»
    grade3: 50              // для «удовлетворительно»
  }
};
