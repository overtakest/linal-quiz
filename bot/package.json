{
  "name": "linal-quiz-bot",
  "version": "2.0.0",
  "description": "АиГ Тесты: Telegram-бот + API проверки теории (Cloudflare Worker + Claude API)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  },
  "engines": {
    "node": ">=18"
  }
}